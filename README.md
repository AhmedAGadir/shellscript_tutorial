to make a file executible:
$ chmod +x myscript.sh

to run the file:
./myscript.sh

to see where bash is:
which bash

shabang at top of file
#! /bin/bash 

to write comments use number signs
# ECHO COMMAND